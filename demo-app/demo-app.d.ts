import { OnInit } from '@angular/core';
import { Router } from '@angular/router';
export declare class DemoApp implements OnInit {
    private router;
    constructor(router: Router);
    ngOnInit(): void;
}
